[package]
name = "donglora-cobs"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "COBS encoder/decoder — no_std, zero-alloc, spec-compliant"

[features]
default = []

[dev-dependencies]
corncobs = "0.1"
rand = "0.8"
