// @generated by build.rs — DO NOT EDIT
pub mod traits;
pub use traits::*;

{% for helper, helper_features in helpers|items -%}
#[cfg(any({% for f in helper_features %}feature = "{{ f }}"{% if not loop.last %}, {% endif %}{% endfor %}))]
mod {{ helper }};
{% endfor %}
cfg_if::cfg_if! {
    {%- for board in boards %}
    {% if loop.first %}if{% else %}else if{% endif %} #[cfg(feature = "{{ board }}")] {
        mod {{ board }};
        pub use {{ board }}::*;
    }
    {%- endfor %}
}
